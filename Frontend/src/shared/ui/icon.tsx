/**
 * SIGGAB — Icon bridge component.
 *
 * Re-exports Lucide icons with a consistent API so that a future migration
 * to React Native only requires changing the import source from
 * `lucide-react` to `lucide-react-native` in this single file.
 */

export {
  // ─── Navigation / Module icons ───
  LayoutDashboard,
  Beef,
  HeartPulse,
  BarChart3,
  Package,
  FileText,
  CheckCircle,
  Shield,
  Users,
  Database,

  // ─── Auth / Session ───
  User,
  Lock,
  Eye,
  EyeOff,
  ArrowRight,
  ShieldCheck,
  LogOut,
  RefreshCw,

  // ─── Action buttons ───
  Plus,
  Pencil,
  Save,
  Trash2,
  X,
  Check,
  XCircle,
  ArrowLeft,
  Search,
  FilterX,
  History,
  UserMinus,

  // ─── Dashboard KPIs / Section titles ───
  Syringe,
  Bell,
  ShoppingCart,
  AlertTriangle,
  ScrollText,

  // ─── Productivo tabs ───
  Layers,
  Baby,
  Scale,
  Milk,

  // ─── Inventario tabs ───
  Tags,
  ArrowLeftRight,
  Receipt,

  // ─── Misc ───
  ChevronDown,
  Calendar,
  Download,
  Loader2,
  Info,
  CircleDot,
  ClipboardList,
  GitCompare,
} from 'lucide-react';

export type { LucideIcon } from 'lucide-react';
